name: hotpath-comment-macos

on:
  workflow_run:
    workflows: ["hotpath-profile-macos"]
    types:
      - completed

permissions:
  contents: read
  pull-requests: write

jobs:
  comment:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - uses: actions/download-artifact@v4
        with:
          name: profile-metrics-macos
          path: /tmp/metrics/
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Install hotpath-utils CLI
        run: cargo install --path crates/hotpath --bin hotpath-utils --features=utils

      - name: Post PR comment - noop timing
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          HEAD_METRICS=$(cat /tmp/metrics/head_noop_timing.json)
          BASE_METRICS=$(cat /tmp/metrics/base_noop_timing.json)
          PR_NUMBER=$(cat /tmp/metrics/pr_number.txt)
          export GITHUB_BASE_REF=$(cat /tmp/metrics/base_ref.txt)
          export GITHUB_HEAD_REF=$(cat /tmp/metrics/head_ref.txt)
          hotpath-utils profile-pr \
            --head-metrics "$HEAD_METRICS" \
            --base-metrics "$BASE_METRICS" \
            --github-token "$GH_TOKEN" \
            --pr-number "$PR_NUMBER" \
            --benchmark-id "noop-timing_macos"

      - name: Post PR comment - alloc timing
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          HEAD_METRICS=$(cat /tmp/metrics/head_alloc_timing.json)
          BASE_METRICS=$(cat /tmp/metrics/base_alloc_timing.json)
          PR_NUMBER=$(cat /tmp/metrics/pr_number.txt)
          export GITHUB_BASE_REF=$(cat /tmp/metrics/base_ref.txt)
          export GITHUB_HEAD_REF=$(cat /tmp/metrics/head_ref.txt)
          hotpath-utils profile-pr \
            --head-metrics "$HEAD_METRICS" \
            --base-metrics "$BASE_METRICS" \
            --github-token "$GH_TOKEN" \
            --pr-number "$PR_NUMBER" \
            --benchmark-id "alloc-timing_macos"

      - name: Post PR comment - alloc alloc
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          HEAD_METRICS=$(cat /tmp/metrics/head_alloc_alloc.json)
          BASE_METRICS=$(cat /tmp/metrics/base_alloc_alloc.json)
          PR_NUMBER=$(cat /tmp/metrics/pr_number.txt)
          export GITHUB_BASE_REF=$(cat /tmp/metrics/base_ref.txt)
          export GITHUB_HEAD_REF=$(cat /tmp/metrics/head_ref.txt)
          hotpath-utils profile-pr \
            --head-metrics "$HEAD_METRICS" \
            --base-metrics "$BASE_METRICS" \
            --github-token "$GH_TOKEN" \
            --pr-number "$PR_NUMBER" \
            --benchmark-id "alloc-alloc_macos"
