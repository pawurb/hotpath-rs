[package]
name = "test-channels-tokio"
version = "0.0.0"
publish = false
edition = "2021"
description = "Tests for Tokio channels instrumentation."

[dependencies]
console-subscriber = { version = "0.5.0", optional = true }
tokio = { version = "1.48.0", features = ["sync", "macros", "rt-multi-thread", "time"] }
hotpath = { workspace = true, features = ["tokio"] }
tracing-subscriber = { version = "0.3", features = ["time", "env-filter"] }
futures-channel = "0.3"
futures-util = "0.3"
rand = "0.9.2"

[features]
hotpath = ["hotpath/hotpath"]
hotpath-alloc = ["hotpath/hotpath-alloc"]
hotpath-cpu = ["hotpath/hotpath-cpu"]
hotpath-meta = ["hotpath/hotpath-meta"]
hotpath-alloc-meta = ["hotpath/hotpath-alloc-meta"]
tokio-console = ["tokio/full", "tokio/tracing", "dep:console-subscriber"]
