[package]
name = "test-rw-lock-std"
version = "0.0.0"
publish = false
edition = "2021"
description = "Tests for std RwLock instrumentation."

[dependencies]
hotpath = { workspace = true }

[features]
hotpath = ["hotpath/hotpath"]
