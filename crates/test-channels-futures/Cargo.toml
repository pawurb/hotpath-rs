[package]
name = "test-channels-futures"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
hotpath = { workspace = true, optional = true, features = ["futures"] }
futures-channel = "0.3"
futures-util = "0.3"
smol = "2.0"
async-io = "2.0"

[features]
hotpath = ["dep:hotpath"]
