[package]
name = "test-all-features"
version = "0.0.0"
publish = false
edition = "2021"
description = "Tests with all feature flags enabled."

[features]
hotpath = ["hotpath/hotpath"]
hotpath-alloc = ["hotpath/hotpath-alloc"]
hotpath-cpu = ["hotpath/hotpath-cpu"]
hotpath-cloud = ["hotpath/hotpath-cloud"]
hotpath-meta = ["hotpath/hotpath-meta"]
hotpath-alloc-meta = ["hotpath/hotpath-alloc-meta"]
hotpath-cloud-meta = ["hotpath/hotpath-cloud-meta"]
hotpath-prometheus = ["hotpath/hotpath-prometheus"]
# Opts in to the examples that are expected to fail the build; only
# `hotpath/tests/unique_labels.rs` enables it.
dup-labels-fixture = []

[[example]]
name = "duplicate_labels_channel"
required-features = ["hotpath", "dup-labels-fixture"]

[[example]]
name = "duplicate_labels_function"
required-features = ["hotpath", "dup-labels-fixture"]

[dependencies]
hotpath = { path = "../hotpath", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
